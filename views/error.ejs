<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - <%= locals.title || 'WhatsApp Service' %></title>
    <link href="/css/tailwind.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="min-h-screen bg-gradient-to-br from-blue-500 via-purple-600 to-purple-800 flex items-center justify-center p-4">
    <div class="bg-white rounded-2xl shadow-2xl p-8 text-center max-w-md w-full transform hover:scale-105 transition-transform duration-300">
        <div class="text-6xl text-red-500 mb-6">
            <i class="fas fa-exclamation-triangle"></i>
        </div>
        <div class="text-4xl font-bold text-gray-700 mb-2">
            <%= locals.status || '500' %>
        </div>
        <div class="text-gray-600 mb-8 text-lg">
            <%= locals.message || 'Something went wrong. Please try again later.' %>
        </div>
        <% if (locals.error && locals.error.stack && process.env.NODE_ENV === 'development') { %>
        <div class="bg-red-50 border border-red-200 rounded-lg p-4 text-left mt-6 mb-6">
            <div class="text-red-800 text-sm font-mono overflow-auto max-h-40">
                <pre><%= error.stack %></pre>
            </div>
        </div>
        <% } %>
        <a href="/" class="inline-flex items-center px-6 py-3 bg-gradient-to-r from-blue-500 to-purple-600 text-white font-semibold rounded-full hover:from-blue-600 hover:to-purple-700 transform hover:-translate-y-1 transition-all duration-200 shadow-lg hover:shadow-xl">
            <i class="fas fa-home mr-2"></i>
            Go Home
        </a>
    </div>
</body>
</html>